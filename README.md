# 月半弯 Half Moon

移动端优先的 PWA 女性生理期记录应用。

## Scripts

```bash
npm install
npm run dev
npm test
npm run build
```

## Scope

- 本地可交互版：周期记录、预测摘要、洞察页、个人页。
- 数据保存在浏览器 `localStorage`，不包含账号、云同步或推送通知。
- UI 采用 React、Vite、TypeScript、Tailwind CSS、shadcn 风格组件和 lucide 图标。
